{% extends 'layout.twig' %}
{% block title %}{{ 'auth.forgot.page_title'|t }} — AnyAuction{% endblock %}

{% block content %}
<div class="container py-5" style="max-width: 28rem;">
    <a href="/" class="d-flex align-items-center gap-2 text-decoration-none text-dark mb-4">
        <span class="aa-brand-badge"><i class="bi bi-hammer"></i></span>
        <span class="fs-5">AnyAuction</span>
    </a>

    <h1 class="h3 mb-1">{{ 'auth.forgot.heading'|t }}</h1>
    <p class="text-muted-aa mb-4">
        {{ 'auth.forgot.subtitle'|t }}
    </p>

    <form method="post" action="/forgot-password" class="d-flex flex-column gap-3">
        <input type="hidden" name="_csrf" value="{{ csrf_token }}">

        <div>
            <label class="form-label small text-muted-aa">{{ 'auth.forgot.email'|t }}</label>
            <div class="input-group">
                <span class="input-group-text"><i class="bi bi-envelope"></i></span>
                <input type="email" name="email" class="form-control"
                       placeholder="{{ 'auth.login.email_placeholder'|t }}"
                       value="{{ old.email|default('') }}" required autofocus>
            </div>
        </div>

        <button type="submit" class="btn btn-primary py-2">
            {{ 'auth.forgot.submit'|t }} <i class="bi bi-arrow-right ms-1"></i>
        </button>
    </form>

    <p class="text-center small text-muted-aa mt-4">
        {{ 'auth.forgot.remembered'|t }} <a href="/login" class="text-decoration-none">{{ 'auth.back_to_signin'|t }}</a>
    </p>
</div>
{% endblock %}
