{% extends 'layout.twig' %}
{% block title %}{{ 'auth.enroll.page_title'|t }} — AnyAuction{% endblock %}

{% block content %}
<div class="container py-5" style="max-width: 28rem;">
    <a href="/" class="d-flex align-items-center gap-2 text-decoration-none text-dark mb-4">
        <span class="aa-brand-badge"><i class="bi bi-hammer"></i></span>
        <span class="fs-5">AnyAuction</span>
    </a>

    {% if step == 'code' %}
        <h1 class="h3 mb-1">{{ 'auth.enroll.title.code'|t }}</h1>
        <p class="text-muted-aa mb-4">
            {% if phone_mask %}
                {{ 'auth.enroll.intro.code'|t({ phone_mask: phone_mask }) }}
            {% else %}
                {{ 'auth.enroll.intro.code_no_mask'|t }}
            {% endif %}
        </p>

        <form method="post" action="/enroll-phone" class="d-flex flex-column gap-3">
            <input type="hidden" name="_csrf" value="{{ csrf_token }}">
            <input type="hidden" name="step" value="code">

            <div>
                <label class="form-label small text-muted-aa">{{ 'auth.enroll.label.code'|t }}</label>
                <div class="input-group">
                    <span class="input-group-text"><i class="bi bi-shield-lock"></i></span>
                    <input type="text" name="code" class="form-control"
                           inputmode="numeric" pattern="\d{6}" maxlength="6"
                           autocomplete="one-time-code"
                           placeholder="123456" required autofocus>
                </div>
            </div>

            <button type="submit" class="btn btn-primary py-2">
                {{ 'auth.enroll.verify_phone'|t }} <i class="bi bi-arrow-right ms-1"></i>
            </button>
        </form>

        <p class="small text-muted-aa mt-3 mb-0">
            <a href="/enroll-phone?step=phone" class="text-decoration-none">{{ 'auth.enroll.use_other'|t }}</a>
        </p>
    {% else %}
        <h1 class="h3 mb-1">{{ 'auth.enroll.title.phone'|t }}</h1>
        <p class="text-muted-aa mb-4">
            {{ 'auth.enroll.intro.phone'|t }}
        </p>

        <form method="post" action="/enroll-phone" class="d-flex flex-column gap-3">
            <input type="hidden" name="_csrf" value="{{ csrf_token }}">
            <input type="hidden" name="step" value="phone">

            <div>
                <label class="form-label small text-muted-aa">{{ 'auth.enroll.label.phone'|t }}</label>
                <div class="input-group">
                    <span class="input-group-text"><i class="bi bi-telephone"></i></span>
                    <input type="tel" name="phone" class="form-control"
                           autocomplete="tel"
                           inputmode="numeric"
                           pattern="[0-9]*"
                           maxlength="11"
                           data-aa-digits-only
                           placeholder="5551234567" required autofocus
                           value="{{ old.phone|default('') }}">
                </div>
                <div class="form-text">{{ 'auth.enroll.help.phone'|t }}</div>
            </div>

            <button type="submit" class="btn btn-primary py-2">
                {{ 'auth.enroll.send_code'|t }} <i class="bi bi-arrow-right ms-1"></i>
            </button>
        </form>
    {% endif %}
</div>

<script>
    document.addEventListener('DOMContentLoaded', function () {
        document.querySelectorAll('[data-aa-digits-only]').forEach(function (el) {
            el.addEventListener('input', function () {
                var stripped = el.value.replace(/[^0-9]/g, '');
                if (stripped !== el.value) el.value = stripped;
            });
        });
    });
</script>
{% endblock %}
